name: sdb_sentinel_master_upc_talker

workflow:
  name: wf-sdb-sentinel-master-upc-talker
  namespace: default

renderer: modular_chaos

targets:
  - id: sdb_sentinels
    finder: by_label
    label: "app.kubernetes.io/instance: dupf-sdb-sentinel"

  - id: sdb_master
    finder: sdb_master

  - id: upc_talker
    finder: upc_talker

  - id: upc_component
    finder: by_label
    label: "app.kubernetes.io/component: dupf-pod-upc"

stages:
  - mode: parallel
    faults:
      - type: network_delay
        selectors:
          from: upc_component
          to: sdb_master
        direction: both
        duration: 30s
        delay:
          latency: 300ms
          jitter: 300ms

      - type: network_loss
        selectors:
          from: upc_component
          to: sdb_master
        direction: both
        duration: 30s
        loss:
          loss: "10"
          correlation: "0"

      - type: network_delay
        selectors:
          from: upc_component
          to: sdb_sentinels
        direction: both
        duration: 30s
        delay:
          latency: 300ms
          jitter: 300ms

      - type: network_loss
        selectors:
          from: upc_component
          to: sdb_sentinels
        direction: both
        duration: 30s
        loss:
          loss: "10"
          correlation: "0"

      - type: pod_kill
        target: sdb_sentinels
        expand:
          mode: random
          count: 1
        delay: 0ms
        duration: 1s

      - type: pod_kill
        target: sdb_master
        delay: 100ms
        duration: 1s

      - type: pod_kill
        target: upc_talker
        delay: 200ms
        duration: 1s

wait_seconds: 40
cleanup: true
